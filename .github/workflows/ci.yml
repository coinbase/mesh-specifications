name: CI

on:
  push:
    branches:
      - master
  pull_request:

env:
  go_version: 1.16

jobs:
  Salus:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          version: latest
      - run: make salus